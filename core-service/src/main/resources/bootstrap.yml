spring:
  application:
    name: core-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      retry:
        max-attempts: 5

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
